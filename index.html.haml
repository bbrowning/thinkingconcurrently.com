---
layout: base
---

- for post in page.posts
  .post
    %h2
      %a{:href => post.url}
        = post.title
    %h3
      = post.date.strftime('%A %B %d')
    .content
      ~post.content
    .comments

= page.posts.links
