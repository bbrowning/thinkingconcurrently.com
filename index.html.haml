---
layout: base
---

- for post in page.posts
  .post
    %h2
      %a{:href => post.url}
        = post.title
    %h3
      = post.date.strftime('%A %B %d %Y')
    .content
      ~post.content
    .comments
      = post.intense_debate_comments_link

= page.posts.links
